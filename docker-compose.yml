version: '3.8'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/usr/src/app/
    ports:
      - 8000:8000

  # bot:
  #   build: 
  #     context: .
  #     dockerfile: Dockerfile_utils
  #   volumes:
  #     - .:/usr/src/utils/
  #   environment:
  #     - PYTHONUNBUFFERED=1
  #   expose:
  #     - 80

